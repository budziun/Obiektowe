Oto implementacja interfejsu `MediaPlayer` oraz dwóch klas `AudioPlayer` i `VideoPlayer`:

```java
// Interfejs MediaPlayer
interface MediaPlayer {
    void play(String trackName);
    void pause();
    String getCurrentTrack();
}

// Klasa AudioPlayer implementująca interfejs MediaPlayer
class AudioPlayer implements MediaPlayer {
    private String currentTrack;

    @Override
    public void play(String trackName) {
        System.out.println("Odtwarzanie audio: " + trackName);
        currentTrack = trackName;
    }

    @Override
    public void pause() {
        System.out.println("Zatrzymano odtwarzanie audio");
    }

    @Override
    public String getCurrentTrack() {
        return currentTrack;
    }
}

// Klasa VideoPlayer implementująca interfejs MediaPlayer
class VideoPlayer implements MediaPlayer {
    private String currentTrack;

    @Override
    public void play(String trackName) {
        System.out.println("Odtwarzanie wideo: " + trackName);
        currentTrack = trackName;
    }

    @Override
    public void pause() {
        System.out.println("Zatrzymano odtwarzanie wideo");
    }

    @Override
    public String getCurrentTrack() {
        return currentTrack;
    }
}

// Klasa MediaPlayerTest do sprawdzenia działania metod
public class MediaPlayerTest {
    public static void main(String[] args) {
        AudioPlayer audioPlayer = new AudioPlayer();
        VideoPlayer videoPlayer = new VideoPlayer();

        // Testowanie AudioPlayer
        audioPlayer.play("Piosenka 1");
        System.out.println("Aktualny utwór: " + audioPlayer.getCurrentTrack());
        audioPlayer.pause();

        // Testowanie VideoPlayer
        videoPlayer.play("Film 1");
        System.out.println("Aktualny utwór: " + videoPlayer.getCurrentTrack());
        videoPlayer.pause();
    }
}
```

W klasie `MediaPlayerTest` utworzono obiekty `AudioPlayer` i `VideoPlayer`, a następnie przetestowano różne metody dla obiektów obu klas. 
